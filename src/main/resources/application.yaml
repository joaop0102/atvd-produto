spring:
  application:
    name: estoque-api

  datasource:
    url: jdbc:h2:file:./data/estoquedb;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

  h2:
    console:
      enabled: true
      path: /h2-console

  rabbitmq:
    host: jackal.rmq.cloudamqp.com
    port: 5671
    username: ksdqhqbz
    password: Vd0Jf4diYyxHTAh49qGjDo7kmwOLKLiT
    virtual-host: ksdqhqbz
    ssl: 
      enabled: true

server:
  port: ${PORT:8080}


app:
  messaging:
    enabled: ${APP_MESSAGING_ENABLED:true}
    exchange: ${APP_MESSAGING_EXCHANGE:produto.exchange}
    queue: ${APP_MESSAGING_QUEUE:produto.eventos}
    routing-key: ${APP_MESSAGING_ROUTING_KEY:produto.evento}